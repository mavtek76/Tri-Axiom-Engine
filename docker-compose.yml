version: "3.8"

services:
  triaxiom:
    build: .
    container_name: triaxiom_proxy
    restart: always
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/docs"]
      interval: 30s
      timeout: 5s
      retries: 3
